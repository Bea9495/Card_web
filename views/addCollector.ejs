<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cartones Mágicos</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/stylesheets/addCard.css" />
    <script
      src="https://kit.fontawesome.com/9b2fedeb7f.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <header><%- include("./includes/navbar") %></header>
    <main>
      <section class="section1 h-100">
        <div class="container-xxl h-100">
          <div class="row h-100">
            <div class="col d-flex justify-content-center align-items-center ">
              <form 
        class="d-flex flex-column"
        action="/collector/addCollector"
        method="post"
        enctype="multipart/form-data"
        >
          <div class="mb-3 text-container">
            <label for="collector_name" class="fw-bold" >Nombre</label>
            <input
            class="border border-0 border-bottom w-100"
              type="text"
              placeholder="nombre"
              name="collector_name"
              id="collector_name"
              value="<%= values.collector_name %>"

            />
            <% validations.forEach ((e)=>{ %>
              <% if(e.path == 'collector_name'){ %>
                <p style="color: red"><%= e.msg %></p>
              <% } %>
            <% }) %>
          </div>
          <div class="mb-3 text-container">
            <label for="interest_in"class="fw-bold" >Interés</label>
            <input
            class="border border-0 border-bottom w-100"
              type="text"
              placeholder="interés"
              name="interest_in"
              id="interest_in"
              value="<%= values.interest_in %>"

            />
            <% validations.forEach ((e)=>{ %>
              <% if(e.path == 'interest_in'){ %>
                <p style="color: red"><%= e.msg %></p>
              <% } %>
            <% }) %>
          </div>
          <div class="mb-3 text-container">
            <label for="email"class="fw-bold" >Email</label>
            <input
            class="border border-0 border-bottom w-100"
              placeholder="email"
              type="text"
              name="email"
              id="email"
              value="<%= values.email %>"

            />
            <% validations.forEach ((e)=>{ %>
              <% if(e.path == 'email'){ %>
                <p style="color: red"><%= e.msg %></p>
              <% } %>
            <% }) %>
          </div>
          <div class="mb-3 text-container">
            <label for="password"class="fw-bold" >Password</label>
            <input
            class="border border-0 border-bottom w-100"
              placeholder="password"
              type="text"
              name="password"
              id="password"
              value="<%= values.password %>"

            />
            <% validations.forEach ((e)=>{ %>
              <% if(e.path == 'password'){ %>
                <p style="color: red"><%= e.msg %></p>
              <% } %>
            <% }) %>
          </div>
          <div class="mb-3 text-container">
            <label for="collection_description" class="fw-bold">Descripción</label>
            <input
            class="border border-0 border-bottom w-100"
              placeholder="descripción"
              type="text"
              name="collection_description"
              id="collection_description"
              value="<%= values.collection_description %>"

            />
          </div>
          <div class="mb-3 text-container">
            <label for="phone"class="fw-bold">Teléfono</label>
            <input
            class="border border-0 border-bottom w-100"
              placeholder="teléfono"
              type="text"
              name="phone"
              id="phone"
              value="<%= values.phone %>"

            />
          </div>
          <div class="m-3 ">
            <label for="foto"><i class="fa-solid fa-images fa-2xl"></i></label>
            <input id="foto" type="file" hidden name="img">
          </div>
          <div class="p-2">
            <h5 style="color:red"><%= typeof message != 'undefined' ? message: null %></h5>
            <button type="submit" class="general-button p-2">Enviar</button>
            <button class="general-button  p-2" onclick="window.location.href='/'" type="reset">Cancelar</button>
            <button class="general-button  p-2" type="reset">Limpiar</button>
            <p class="m-1">¿Ya estas registrado? <a href="/collector/login">Login aqui</a> </p>
          </div>
        </form>
            </div>
          </div>
        </div>
      </section>
    </main>

    <%- include("./includes/footer") %>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
